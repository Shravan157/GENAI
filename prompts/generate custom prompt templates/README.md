# Custom Prompt Templates

## What is this?
This folder creates a reusable medical assistant prompt template, saves it, and loads it again. It separates prompt creation from prompt usage. In short: prompt templates can be versioned like normal files.

## Why does it exist?
- Use when your prompt is large, reusable, or shared across scripts.
- Use this README when you open this folder and want a quick overview before reading code.

## Concepts Covered
| Concept | What it does | When to use it |
|---|---|---|
| Python example script | Contains runnable learning code. | Use when studying the implementation. |
| Saved prompt/schema data | Stores reusable configuration or prompt data. | Use when a Python script loads data from disk. |
| Prompt templates | Reusable instructions with variables. | Use when the same prompt structure needs different inputs. |
| JSON schema/output | Defines language-neutral structured output. | Use when data should be JSON-compatible. |

## Files in this Folder

### Files
- `medical-chatbot-template.py`: Creates a large medical assistant PromptTemplate and saves it as JSON.
- `medical_prompt_template.json`: Saved LangChain prompt template generated by the template script.
- `test-created-template.py`: Loads the saved prompt template and invokes a model with filled variables.

## How to Read This Folder
1. Start with the smallest Python file in this folder.
2. Identify the LangChain object being demonstrated.
3. Follow the data flow from input, to prompt/loader, to model/parser, to printed output.

## Common Mistakes / Gotchas
- Putting all instructions into one hard-coded string instead of using variables.
- Forgetting that chat prompts should separate system and human messages.

## Interview Questions
**Q1. What is the main idea of this folder?**
This folder creates a reusable medical assistant prompt template, saves it, and loads it again. It separates prompt creation from prompt usage.

**Q2. When would you use this in a real project?**
Use when your prompt is large, reusable, or shared across scripts.
